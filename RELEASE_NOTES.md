# QMem v1.0.0

QMemの最初の安定版です。

## 主な機能

- 開いてすぐ書けるプレーンテキストのメモ画面
- 入力後180msでローカルSQLiteへ自動保存
- `Cmd+K`による過去メモの本文検索
- `Option+Space`による、ほかのアプリからの新規メモ呼び出し
- `Cmd+W`と`Cmd+Q`の前に保留中の入力を保存
- 空白だけのメモを保存せず、本文を消した過去メモを削除

## インストール

お使いのMacに合うDMG（Apple SiliconまたはIntel）をダウンロードし、QMemを「アプリケーション」フォルダへ移してください。

## 現在の制限

- 対応OSはmacOSです。
- クラウド同期とMarkdownプレビューはありません。
- QMemを`Cmd+Q`で完全終了すると、`Option+Space`では呼び出せません。
- Developer IDによる署名・Appleの公証には対応していません。
