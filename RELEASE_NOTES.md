# QMem v1.2.0

必要なときだけ開く設定画面を追加し、QMemの起動方法、ショートカット、本文の読みやすさを変更できるようにしました。

## 変更点

- `Cmd+,`で、検索と同じオーバーレイ形式の設定を開けるようにしました。
- 「ログイン時に起動」を追加しました。有効にすると、ログイン後にウィンドウを表示せずQMemを起動します。
- 「メニューバーに常駐」を追加しました。有効にするとDockの代わりにメニューバーへQMemのアイコンを表示し、アイコンから新しいメモを開くか完全終了できます。
- 呼び出し、新規メモ、検索の3つのショートカットを設定から変更できるようにしました。
- 本文の文字サイズを15〜24pxの範囲で変更できるようにしました。
- 設定からGitHub Releasesの新しいバージョンを手動で確認できるようにしました。更新の自動インストールは行いません。

## 改善・修正

- 設定項目同士の余白を広げ、項目のまとまりを読み取りやすくしました。
- 設定画面右上の`Esc`表示を削除しました。キーボードの`Esc`では引き続き設定を閉じられます。
- ショートカット入力中にフォーカスが外れ、変更できないことがある問題を修正しました。
- メニューバーでQMemのアイコンが見えない問題を修正し、表示専用のモノクロアイコンを追加しました。

## インストール

お使いのMacに合うDMG（Apple SiliconまたはIntel）をダウンロードし、QMemを「アプリケーション」フォルダへ移してください。

## 現在の制限

- 対応OSはmacOSです。
- クラウド同期とMarkdownプレビューはありません。
- QMemを`Cmd+Q`で完全終了すると、`Option+Space`では呼び出せません。
- 更新は設定から確認できますが、ダウンロードとインストールは手動です。
- Developer IDによる署名・Appleの公証には対応していません。
